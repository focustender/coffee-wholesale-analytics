# Reusable components log

Running inventory of tooling built during this project (or others) that's
designed to be copied into future projects rather than rebuilt from scratch.
Update this whenever a new reusable piece is built, whether or not it
started life here.

| Component | Lives at | What it does | Status | Built for | Also usable in |
|---|---|---|---|---|---|
| Salesforce CLI setup runbook | `docs/reference/salesforce-cli-setup.md` (this repo) | Step-by-step for connecting a project to Salesforce via the `sf` CLI, working around the broken claude.ai Salesforce Beta MCP connector. Covers install gotchas, login, and the token-redaction split. | Done, verified working (2026-09-09) | Coffee Wholesale Analytics | Any future project needing Salesforce data access |
| `salesforce_client.py` | `integrations/salesforce_client.py` (this repo) | Thin Salesforce REST client (query/create/update) authenticated via the `sf` CLI at runtime, never hardcodes credentials. | Done, smoke-tested against a real Developer Edition org | Coffee Wholesale Analytics | Any future project — copy the file as-is, no project-specific code in it |
| `generate_dataset.py` pattern (stdlib-only, seeded, weighted-distribution synthetic generator) | `data/generate_dataset.py` (this repo), originally from `~/fireclayTile/trade-signal/data/generate_dataset.py` | The general approach (not the file itself, since fields are business-specific): stdlib-only Python, fixed random seed, weighted categorical distributions, fabricated `@example.com` emails. | Reused once already (Trade Signal to this project) | Trade Signal (Fireclay) | Any future synthetic-dataset project — reuse the *pattern*, not the code |
| HubSpot API setup runbook | `docs/reference/hubspot-api-setup.md` (this repo) | Step-by-step for a durable, script-usable HubSpot credential (Service Key, the 2026 successor to Private Apps) plus the "one HubSpot developer test account per project" lesson learned the hard way when this project first pointed at a portal already near its 1,000-contact cap from Trade Signal. | Done, verified working (2026-09-10) | Coffee Wholesale Analytics | Any future project needing HubSpot data access |
| `hubspot_client.py` | `integrations/hubspot_client.py` (this repo) | Thin HubSpot CRM REST client (contacts search/create/update, custom property creation) authenticated via `HUBSPOT_ACCESS_TOKEN` at runtime, never hardcodes credentials. | Done, used to seed 500 real contacts | Coffee Wholesale Analytics | Any future project — copy as-is |
| Shopify API setup runbook | `docs/reference/shopify-api-setup.md` (this repo) | Step-by-step for Shopify's 2026 Dev Dashboard app model: Client ID/Secret exchanged at runtime for a short-lived token (client credentials grant), plus the Protected Customer Data approval gate that blocks customer PII access even for single-store custom apps. | Done, verified working (2026-09-10) | Coffee Wholesale Analytics | Any future project needing Shopify data access |
| `shopify_client.py` | `integrations/shopify_client.py` (this repo) | Thin Shopify Admin GraphQL client (customer search/create/list) that performs its own client-credentials token exchange and caches the result in memory, authenticated via `SHOPIFY_STORE_DOMAIN`/`SHOPIFY_CLIENT_ID`/`SHOPIFY_CLIENT_SECRET`. | Done, used to seed 500 real customers | Coffee Wholesale Analytics | Any future project — copy as-is |
| `reconcile_lifecycle.py` (generic reconciliation function) | `analysis/reconcile_lifecycle.py` (this repo) | A generic `reconcile(source, target, key_field, ...)` function: diffs two lifecycle-bearing record sets by a shared key, with an optional value map for vocabulary translation. Not specific to HubSpot, Shopify, or this project's schema. | Done, applied in `analysis/reconcile_hubspot.py` | Coffee Wholesale Analytics | Any future cross-system reconciliation — this is the one most worth reusing as-is |

## Planned, not built yet

- **Shopify order-level reconciliation**: the current reconciliation only covers HubSpot's lifecyclestage. Shopify integration is customer-only (see `docs/reference/shopify-api-setup.md` for why order seeding was out of scope) — a future pass could compare Shopify's `orders_count`/`total_spent` against the local ground truth if real order seeding gets built.
